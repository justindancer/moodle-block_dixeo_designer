# AJAX endpoints (non–web-service scripts)

Small `AJAX_SCRIPT` entry points used by `amd/src/generator.js` for multipart upload and single-file delete. They are **not** Moodle web services (`db/services.php`).

Persistence and orchestration live under `classes/service/submission/` and `classes/service/designer_submission_ui_service.php`.
